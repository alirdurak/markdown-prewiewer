import { createSlice } from "@reduxjs/toolkit"; 
    const initialState = {

    }

const markdownSlice = createSlice({
    name:"markdown",
    initialState,
    reducers:{

    }
})
expor default markdownSlice.reducer